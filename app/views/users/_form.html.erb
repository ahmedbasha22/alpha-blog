<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body class= <%= @user.new_record? ? "sign-body" : "site-body" %>>
    <div class='row form signup-frame'>
      <div class='col-md-12'>
        <%= form_for @user do |f| %>
        <h1 class="sign-text" align="center"> <%= @user.new_record? ? "Sign-up for Alpha-Blog" : "Edit your account" %> </h1>
          <div class="form-group" >
            <div class="control-label col-sm-2 signup">
              <%= f.label :username %>
            </div>
            <div class='col-sm-12'>
              <%= f.text_field :username, class: "form-control",  align: "center", placeholder: "Enter username", autofocus: true %>
            </div>
          </div>
          <div class="form-group">
            <div class="control-label col-sm-2 signup">
              <%= f.label :email %>
            </div>
            <div class='col-sm-12'>
              <%= f.email_field :email, class: "form-control", placeholder: "Enter email" %>
            </div>
          </div>
          <div class="form-group">
            <div class="control-label col-sm-2 signup">
              <%= f.label :password %>
            </div>
            <div class='col-sm-12'>
              <%= f.password_field :password, class: "form-control", placeholder: "Enter password" %>
            </div>
          </div>
          <div class="form-group" align="center">
            <div class="col-sm-10">
              <%= f.submit (@user.new_record? ? "Sign-up" : "Update your account"), class: "btn btn-lg my_active_btn"%>
            </div>
          </div>
        <% end %>
        <div class="form-group" align='center'>
          <div class="col-sm-10">
            <%= link_to 'Cancel Request and return to articles listing', articles_path %>
          </div>
        </div>
      </div>
    </div>
    <% if @user.errors.any? %>
      <div class="alert alert-danger" id="alert_body" role="alert">
        <div>
          <h2>
            <%= pluralize(@user.errors.count, "errors") %>
            prohibited this article from being saved:
          </h2>
          <div>
            <ul>
              <% @user.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    <% end %>
  </body>
</html>
